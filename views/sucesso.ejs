<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="icon-red.svg" type="image/svg+xml">
    <title>Confirmação de Matrícula TECHERS</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #6a1b9a;
            --primary-light: #9c4dcc;
            --primary-dark: #38006b;
            --secondary: #2d2d2d;
            --success: #28a745;
            --light: #f8f9fa;
            --white: #ffffff;
        }

        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #eef1f5 100%);
            font-family: 'Poppins', 'Segoe UI', system-ui, sans-serif;
            min-height: 100vh;
        }

        .confirmation-card {
            background: var(--white);
            border-radius: 16px;
            padding: 3rem;
            margin: 2rem auto;
            box-shadow: 0 8px 30px rgba(0,0,0,0.08);
            max-width: 800px;
        }

        .confirmation-icon {
            font-size: 4rem;
            color: var(--success);
            margin-bottom: 1.5rem;
            animation: pulse 1.5s infinite;
        }

        .status-badge {
            padding: 0.6rem 1.5rem;
            border-radius: 100px;
            font-size: 1.1rem;
            background-color: rgba(40, 167, 69, 0.1);
            color: var(--success);
            display: inline-block;
            margin-bottom: 1.5rem;
        }

        .detail-list {
            list-style: none;
            padding: 0;
            margin: 2rem 0;
        }

        .detail-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem;
            margin: 0.5rem 0;
            background: rgba(106, 27, 154, 0.03);
            border-radius: 8px;
        }

        .detail-label {
            color: var(--primary);
            font-weight: 500;
        }

        .detail-value {
            color: var(--secondary);
            font-weight: 400;
        }

        .btn-primary {
            background-color: var(--primary);
            color: white;
            padding: 0.75rem 2rem;
            border-radius: 8px;
            font-weight: 600;
            transition: all 0.2s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .btn-primary:hover {
            background-color: var(--primary-dark);
            transform: translateY(-2px);
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }

        @media (max-width: 768px) {
            .confirmation-card {
                padding: 2rem;
                margin: 1rem;
            }
            
            .confirmation-icon {
                font-size: 3rem;
            }
        }
    </style>
</head>
<body>
    <!-- Include the Navbar -->
    <%- include('includes/navbarpais') %>

    <div class="container my-5">
        <div class="confirmation-card text-center">
            <div class="confirmation-icon">
                <i class="fas fa-check-circle"></i>
            </div>
            <span class="status-badge">Confirmado</span>
            <h1 class="text-primary mb-3">Matrícula Confirmada!</h1>
            <p class="lead text-muted mb-4">Sua rematrícula foi processada com sucesso para 2026</p>

          

            <a href="/" class="btn-primary">
                <i class="fas fa-home"></i>
                Voltar ao Início
            </a>
        </div>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>