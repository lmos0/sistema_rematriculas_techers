<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="icon-red.svg" type="image/svg+xml">
    <title>Lista de Turmas TECHERS</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <!-- Include Navbar -->
    <%- include('includes/navbar') %>

    <div class="container my-5">
        <h1 class="text-center mb-4">Lista de Turmas TECHERS</h1>

       

        <div class="table-responsive">
            <table class="table table-bordered table-striped text-center">
                <thead class="table-dark">
                    <tr>
                        <th>Nome da Turma</th>
                        <th>Curso</th>
                        <th>Nível</th>
                        <th>Modalidade</th>
                        <th>Dia da Semana</th>
                        <th>Horário</th>
                        <th>Vagas</th>
                        <th>Ações</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (turmas.length > 0) { %>
                        <% turmas.forEach(function(turma) { %>
                            <tr>
                                <td><%= turma.nome %></td>
                                <td><%= turma.curso %></td>
                                <td><%= turma.level %></td>
                                <td><%= turma.modalidade %></td>
                                <td><%= turma.dia_semana %></td>
                                <td><%= turma.horario %></td>
                                <td><%= turma.vagas %></td>
                                <td>
                                    <a href="/turmas/<%= turma.id %>/edit" class="btn btn-warning btn-sm">Editar</a>
                                    <form action="/turmas/<%= turma.id %>?_method=DELETE" method="POST" class="d-inline">
                                        <button type="submit" class="btn btn-danger btn-sm">Excluir</button>
                                    </form>
                                </td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="8" class="text-center">Nenhuma turma encontrada no sistema.</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>

        <!-- Pagination controls -->
       
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
