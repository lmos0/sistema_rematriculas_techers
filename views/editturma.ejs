<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="icon-red.svg" type="image/svg+xml">
    <title>Editar Turma TECHERS</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #6a1b9a;
            --primary-light: #9c4dcc;
            --primary-dark: #38006b;
            --secondary: #2d2d2d;
            --light: #f8f9fa;
            --white: #ffffff;
            --danger: #dc3545;
            --warning: #ffc107;
            --success: #28a745;
        }

        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #eef1f5 100%);
            font-family: 'Poppins', 'Segoe UI', system-ui, sans-serif;
            color: var(--secondary);
        }

        .form-card {
            background: var(--white);
            border-radius: 16px;
            padding: 2rem;
            margin: 2rem auto;
            box-shadow: 0 8px 30px rgba(0,0,0,0.08);
            max-width: 800px;
        }

        .form-title {
            color: var(--primary);
            font-weight: 700;
            margin-bottom: 2rem;
            font-size: 1.75rem;
        }

        .form-control {
            border: 2px solid rgba(106, 27, 154, 0.1);
            border-radius: 8px;
            padding: 0.75rem 1rem;
            transition: all 0.2s ease;
        }

        .form-control:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(106, 27, 154, 0.15);
        }

        .form-label {
            font-weight: 600;
            color: var(--primary-dark);
            margin-bottom: 0.5rem;
        }

        .current-value {
            display: inline-block;
            margin-top: 0.5rem;
            padding: 0.25rem 0.75rem;
            background: rgba(106, 27, 154, 0.1);
            border-radius: 6px;
            color: var(--primary);
            font-size: 0.9rem;
        }

        .btn-primary {
            background-color: var(--primary);
            color: white;
            padding: 0.75rem 1.5rem;
            font-weight: 600;
            border-radius: 8px;
            transition: all 0.2s ease;
        }

        .btn-primary:hover {
            background-color: var(--primary-dark);
            transform: translateY(-1px);
        }

        .select-icon {
            position: relative;
        }

        .select-icon::after {
            content: "\f078";
            font-family: "Font Awesome 6 Free";
            font-weight: 900;
            position: absolute;
            right: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: var(--primary);
            pointer-events: none;
        }

        @media (max-width: 768px) {
            .form-card {
                margin: 1rem;
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <!-- Include Navbar -->
    <%- include('includes/navbar') %>

    <div class="container my-5">
        <div class="form-card">
            <h1 class="form-title text-center">Editar Turma</h1>
            
            <form action="/admin/turmas/editar/<%= turma.id %>" method="POST" class="needs-validation" novalidate>
                <div class="row g-4">
                    <div class="col-12">
                        <label for="nome" class="form-label">Nome da Turma</label>
                        <input type="text" class="form-control" name="nome" id="nome" 
                               value="<%= turma.nome %>" required>
                        <div class="current-value">Atual: <%= turma.nome %></div>
                    </div>

                    <div class="col-md-6">
                        <label for="curso" class="form-label">Curso</label>
                        <select name="curso" class="form-select" required>
                            <option value="Programação" <%= turma.curso === 'Programação' ? 'selected' : '' %>>Programação</option>
                            <option value="Robótica" <%= turma.curso === 'Robótica' ? 'selected' : '' %>>Robótica</option>
                            <option value="Animação Digital" <%= turma.curso === 'Animação Digital' ? 'selected' : '' %>>Animação Digital</option>
                            <option value="Maker" <%= turma.curso === 'Maker' ? 'selected' : '' %>>Maker</option>
                            <option value="Design Gráfico" <%= turma.curso === 'Design Gráfico' ? 'selected' : '' %>>Design Gráfico</option>
                            <option value="Video Making" <%= turma.curso === 'Video Making' ? 'selected' : '' %>>Video Making</option>
                        </select>
                        <div class="current-value">Atual: <%= turma.curso %></div>
                    </div>

                    <div class="col-md-6">
                        <label for="level" class="form-label">Level</label>
                        <input type="number" class="form-control" name="level" id="level" 
                               value="<%= turma.level %>" min="1" max="8" required>
                        <div class="current-value">Atual: <%= turma.level %></div>
                    </div>

                    <div class="col-md-6 ">
                        <label for="faixa_etaria" class="form-label">Faixa Etária</label>
                        <select name="faixa_etaria" class="form-select" required>
                            <option value="kids" <%= turma.faixa_etaria === 'kids' ? 'selected' : '' %>>Kids (6 - 8 anos)</option>
                            <option value="jr1" <%= turma.faixa_etaria === 'jr1' ? 'selected' : '' %>>JR1 (8 - 10 anos)</option>
                            <option value="jr2" <%= turma.faixa_etaria === 'jr2' ? 'selected' : '' %>>JR2 (10 - 12 anos)</option>
                            <option value="teens" <%= turma.faixa_etaria === 'teens' ? 'selected' : '' %>>Teens (13 - 17 anos)</option>
                            <option value="adulto" <%= turma.faixa_etaria === 'adulto' ? 'selected' : '' %>>Adulto (18+ anos)</option>
                        </select>
                        <div class="current-value">Atual: <%= turma.faixa_etaria %></div>
                    </div>

                    <div class="col-md-6 ">
                        <label for="modalidade" class="form-label">Modalidade</label>
                        <select name="modalidade" class="form-select" required>
                            <option value="Presencial" <%= turma.modalidade === 'Presencial' ? 'selected' : '' %>>Presencial</option>
                            <option value="Online" <%= turma.modalidade === 'Online' ? 'selected' : '' %>>Online</option>
                            <option value="Híbrida" <%= turma.modalidade === 'Híbrida' ? 'selected' : '' %>>Híbrida</option>
                        </select>
                        <div class="current-value">Atual: <%= turma.modalidade %></div>
                    </div>

                    <div class="col-md-6 ">
                        <label for="dia_semana" class="form-label">Dia da Semana</label>
                        <select class="form-select" name="dia_semana" required>
                            <option value="Terça-feira" <%= turma.dia_semana === 'Terça-feira' ? 'selected' : '' %>>Terça-feira</option>
                            <option value="Quarta-feira" <%= turma.dia_semana === 'Quarta-feira' ? 'selected' : '' %>>Quarta-feira</option>
                            <option value="Quinta-feira" <%= turma.dia_semana === 'Quinta-feira' ? 'selected' : '' %>>Quinta-feira</option>
                            <option value="Sexta-feira" <%= turma.dia_semana === 'Sexta-feira' ? 'selected' : '' %>>Sexta-feira</option>
                            <option value="Sábado" <%= turma.dia_semana === 'Sábado' ? 'selected' : '' %>>Sábado</option>
                        </select>
                        <div class="current-value">Atual: <%= turma.dia_semana %></div>
                    </div>

                    <div class="col-md-6">
                        <label for="horario" class="form-label">Horário</label>
                        <input type="time" class="form-control" name="horario" id="horario" 
                               value="<%= turma.horario %>" required>
                        <div class="current-value">Atual: <%= turma.horario %></div>
                    </div>

                    <div class="col-12">
                        <label for="vagas" class="form-label">Vagas Disponíveis</label>
                        <input type="number" class="form-control" name="vagas" id="vagas" 
                               value="<%= turma.vagas %>" min="1" required>
                        <div class="current-value">Atual: <%= turma.vagas %></div>
                    </div>

                    <div class="col-12 text-center mt-4">
                        <button type="submit" class="btn btn-primary px-5">
                            <i class="fas fa-save me-2"></i>Salvar Alterações
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        // Form validation
        (() => {
            'use strict'
            const forms = document.querySelectorAll('.needs-validation')
            
            Array.from(forms).forEach(form => {
                form.addEventListener('submit', event => {
                    if (!form.checkValidity()) {
                        event.preventDefault()
                        event.stopPropagation()
                    }
                    form.classList.add('was-validated')
                }, false)
            })
        })()
    </script>
</body>
</html>