<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="icon-red.svg" type="image/svg+xml">
    <title>Confirmar Rematrícula</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Container background */
        body {
            background-color: #f8f9fa;
        }
        /* Card Styling */
        .card {
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        /* Form Section Titles */
        .form-section-title {
            font-weight: bold;
            color: #343a40;
            margin-top: 1.5rem;
        }
        /* Confirm Button */
        .btn-primary {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            font-size: 1rem;
        }
    </style>
</head>
<body>
    <%- include('includes/navbar') %>

    <h3 class="text-center mt-4">Confirmar Rematrícula</h3>
    <p class="text-center text-muted">Confira e confirme as informações para 2025</p>
    
    <div class="container my-5">
        <div class="card mb-4">
            <div class="card-body">
                <h3 class="card-title text-center"><%= aluno.nome_aluno %></h3>
                <ul class="list-unstyled">
                    <li><strong>Responsável:</strong> <%= aluno.nome_responsavel %></li>
                    <li><strong>CPF do Responsável:</strong> <%= aluno.cpf_responsavel %></li>
                    <li><strong>Valor do curso para 2025:</strong> <%= aluno.valor_2025.toLocaleString('pt-BR', {style: 'currency', currency: 'BRL'}) %> (Em até 12x sem juros)</li>
                    <li><strong>Curso Atual:</strong> <%= aluno.curso %></li>
                </ul>
            </div>
        </div>

        <form action="" method="post">
            <p class="form-section-title">Modalidade de Ensino</p>
            <div class="mb-3">
                <label for="modalidade" class="form-label">Escolha a modalidade das aulas para 2025</label>
                <form action="" method="post">
                <select name="modalidade" id="modalidade" class="form-select" required>
                    <option value="" disabled hidden>Selecione a Modalidade da Turma</option>
                    <option value="Presencial">Presencial</option>
                    <option value="Online">Online</option>
                </select>
                <div class="d-flex justify-content-center mt-2">
                <button type="submit" class="btn btn-primary">Buscar</button>
            </div>
            </form>
            </div>

        

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.querySelector('form').addEventListener('submit', function() {
            const spinner = document.querySelector('.spinner-border');
            const button = document.querySelector('.btn-primary');
            spinner.classList.remove('d-none');  // Show spinner
            button.disabled = true;              // Disable button to prevent multiple submissions
        });
    </script>
</body>
</html>
