<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="icon-red.svg" type="image/svg+xml">
    <title>Confirmar Rematrícula</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .card {
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .form-section-title {
            font-weight: bold;
            color: #343a40;
            margin-top: 1.5rem;
        }
        .btn-primary {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            font-size: 1rem;
        }
    </style>
</head>
<body>
    <%- include('includes/navbar') %>

    <h3 class="text-center mt-4">Confirmar Rematrícula</h3>
    <p class="text-center text-muted">Confira e confirme as informações para 2025</p>

    <div class="container my-5">
        <div class="card mb-4">
            <div class="card-body">
                <h3 class="card-title text-center"><%= aluno.nome_aluno %></h3>
                <ul class="list-unstyled">
                    <li><strong>Responsável:</strong> <%= aluno.nome_responsavel %></li>
                    <li><strong>CPF do Responsável:</strong> <%= aluno.cpf_responsavel %></li>
                    <li><strong>Valor do curso para 2025:</strong> <%= aluno.valor_2025.toLocaleString('pt-BR', {style: 'currency', currency: 'BRL'}) %> (Em até 12x sem juros)</li>
                    <li><strong>Curso Atual:</strong> <%= aluno.curso %></li>
                    <% if (aluno.segundo_curso) { %>
                        <li><strong>Segundo Curso:</strong> <%= aluno.segundo_curso %></li>
                    <% } %>
                </ul>
            </div>
        </div>

        <form action="" method="post">
            <h5 class="form-section-title">Modalidade de Ensino - Curso de <%= aluno.curso %> </h5>
            <div class="mb-3">
                <label for="modalidade" class="form-label">Escolha a modalidade das aulas para 2025</label>
                <select name="modalidade" id="modalidade" class="form-select" required>
                    <% if (aluno.curso === 'Maker' || aluno.curso === 'Robótica') { %>
                        <option value="Presencial">Presencial</option>
                    <% } else { %>
                        <option value="Presencial">Presencial</option>
                        <option value="Online">Online</option>
                    <% } %>
                </select>
            </div>
        
            <% if (aluno.segundo_curso) { %>
                <h5 class="form-section-title">Modalidade de Ensino - Curso de <%= aluno.segundo_curso %> </h5>
                <div class="mb-3">
                    <label for="modalidade_segundo_curso" class="form-label">Escolha a modalidade das aulas para 2025</label>
                    <select name="modalidade_segundo_curso" id="modalidade_segundo_curso" class="form-select" required>
                        <% if (aluno.segundo_curso === 'Maker' || aluno.segundo_curso === 'Robótica') { %>
                            <option value="Presencial">Presencial</option>
                        <% } else { %>
                            <option value="Presencial">Presencial</option>
                            <option value="Online">Online</option>
                        <% } %>
                    </select>
                </div>
            <% } %>
        
         
      

            <!-- Buttons Section -->
            <div class="d-flex justify-content-between mt-4">
                <!-- Back Button -->
                <button type="button" class="btn btn-secondary" onclick="history.back();">Voltar</button>

                <!-- Confirm Button with Spinner -->
                <button type="submit" class="btn btn-primary">
                    <span>Confirmar</span>
                    <div class="spinner-border spinner-border-sm text-light d-none" role="status"></div>
                </button>
            </div>
        </form>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // document.querySelector('form').addEventListener('submit', function() {
        //     const spinner = document.querySelector('.spinner-border');
        //     const button = document.querySelector('.btn-primary');
        //     spinner.classList.remove('d-none');
        //     button.disabled = true;
        // });
    </script>
</body>
</html>
